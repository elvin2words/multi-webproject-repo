```
echo " " >> README.md
git init
git add .
git commit -m "Initial commit for multiple projects parent folder"
git branch -M master
git remote add origin https://github.com/elvin2words/multi-webproject-repo.git
git push -u origin master

git add .
git commit -m "another project update"
git branch -M master
git push -u origin master
```
